console.log('i a la k')
var suma=0;
var k=10;
for(i=1;i<=10;i++){
 
    suma=suma+(Math.pow(i,k));
}
close= (1/(k+1))*(Math.pow(10,(k+1)));

console.log('Resultado sumnatoria '+suma+'\n Resultado forma cerrada: '+close);
